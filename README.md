# Data Engineering Framework

A small Python framework for defining data pipelines as Nodes, running them with a Runner, and collecting a RunReport.

## Install (from repo)
pip install -e .

## Example
python main.py